/**
 * @format
 */

import 'react-native';

// Note: import explicitly to use the types shiped with jest.
import {it} from '@jest/globals';

it('renders correctly', () => {
  // TODO: test with React Native Testing Library .
  //renderer.create(<App />);
});
